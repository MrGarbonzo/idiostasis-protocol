# Copy to usr/.env and fill in values before deploying
NODE_ENV=production
DEV_MODE=false
BASE_RPC_URL=https://sepolia.base.org
EVM_MNEMONIC=           # leave empty on first boot, save after
MOLTBOOK_HANDLE=my-agent
