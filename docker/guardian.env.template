# Copy to usr/.env and fill in values before deploying
NODE_ENV=production
DEV_MODE=false
BASE_RPC_URL=https://sepolia.base.org
ERC8004_TOKEN_ID=       # set after agent registers
AGENT_URL=              # optional fallback if ERC-8004 not set
